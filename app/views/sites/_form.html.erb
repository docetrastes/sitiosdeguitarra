<%= form_for @site do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </p>
  <p>
    <%= f.label :tag_names, "Tags (comma separated)" %><br />
    <%= f.text_field :tag_names %>
  </p>
  <p>
    <span>Utiliza la etiqueta <tt>[read_more]</tt> en cualquier parte para ocultar el contenido que sigue y crear un link que dice "Leer más sobre...".</span><br />
    <%= f.label :review %><br />
    <%= f.text_area :review %>
  </p>
  <p><%= f.submit %></p>
<% end %>
